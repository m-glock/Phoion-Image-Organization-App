<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:DLuOvBamG.ViewModels"
             mc:Ignorable="d"
             x:Class="DLuOvBamG.Views.ScanResultPage"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <vm:ScanResultViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource bgColor}"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout x:Name="StackLayout" Padding="10,50,0,10">
        <!-- content is dynamically generated and inserted -->
    </StackLayout>

</ContentPage>